[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-whatsapp-receptionist"
version = "0.1.0"
description = "AI-powered WhatsApp Receptionist for SMBs"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["src"]